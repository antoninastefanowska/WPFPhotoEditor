<Window x:Class="PhotoEditor.MaskValuesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhotoEditor"
        mc:Ignorable="d"
        Title="Podaj wartości maski" SizeToContent="WidthAndHeight" Loaded="Window_OnLoaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Margin="5" Grid.ColumnSpan="2" x:Name="ValuesGrid" />
        <Button Grid.Row="1" Margin="5" Padding="5" Click="ButtonCancel_OnClick">Anuluj</Button>
        <Button Grid.Row="1" Grid.Column="1" Margin="5" Padding="5" Click="ButtonOK_OnClick">OK</Button>
    </Grid>
</Window>
